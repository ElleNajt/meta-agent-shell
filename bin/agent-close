#!/bin/bash
# agent-close - Close an agent session
# Usage: agent-close "buffer-name"

if [ $# -lt 1 ]; then
    echo "Usage: agent-close \"buffer-name\"" >&2
    exit 1
fi

target="$1"

emacsclient --eval "(meta-agent-shell-close-session \"$target\" $$)"
